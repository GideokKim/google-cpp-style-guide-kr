# 0과 nullptr/NULL (0 and nullptr/NULL)

## 원문 규칙 번역

포인터에는 nullptr을 사용하고 문자에는 '\0'을 사용합니다(0 리터럴은 아님).

포인터(주소 값)의 경우 유형 안전성을 제공하므로 nullptr을 사용하십시오.

널 문자에는 '\0'을 사용하십시오. 올바른 유형을 사용하면 코드를 더 쉽게 읽을 수 있습니다.

---

---

## 이해하기 쉽게 설명하기

이 규칙의 핵심은 포인터(주소 값)의 경우 유형 안전성을 제공하므로 nullptr을 사용하십시오.

실제로 코드를 볼 때는 포인터에는 nullptr을 사용하고 문자에는 '\0'을 사용합니다(0 리터럴은 아님).

점검할 때는 특히 다음을 확인하세요:

- 올바른 유형을 사용하면 코드를 더 쉽게 읽을 수 있습니다.
