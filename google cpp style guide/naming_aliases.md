# 별칭 이름 (Aliases)

별칭 이름은 원래 이름이 나타나는 위치가 아닌 별칭이 정의된 컨텍스트에 적용되는 다른 새 이름과 동일한 원칙을 따릅니다.

---

## 이해하기 쉽게 설명하기

### 핵심: 별칭이 "정의된 위치"의 규칙을 따른다

별칭 이름은 다른 새 이름과 같은 원칙을 따릅니다 — 단, 기준은 **원래 이름이 있던 위치가 아니라 별칭이 정의된 문맥**입니다.

즉, 타입 별칭이면 타입 이름 규칙(PascalCase), 상수 별칭이면 상수 규칙(`k` 접두사)을 그 별칭이 선언된 자리에 맞춰 적용합니다.
